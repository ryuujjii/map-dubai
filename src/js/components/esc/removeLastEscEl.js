import { escCollection } from '@/components/globals';

export default function removeLastEscEl(el) {
  // const exception = 'esc';
  // const removeLast = el === exception || escCollection.at(-1) === el;
  // if (!removeLast) {
  //   return;
  // }
  // return escCollection.pop();
};
